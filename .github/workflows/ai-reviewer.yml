name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  ai-pr-review:
    uses: Open-Paws/Open-Paws-Automations/.github/workflows/ai-pr-review.yml@main
    secrets: inherit

