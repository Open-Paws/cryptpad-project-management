name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  ai-review:
    uses: Open-Paws/Open-Paws-Automations/.github/workflows/ai-pr-review.yml@thekaailashsharma-patch-1
